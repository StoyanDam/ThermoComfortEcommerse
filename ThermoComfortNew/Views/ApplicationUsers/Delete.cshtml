
 @model ThermoComfortNew.Domain.ApplicationUser

@{
    ViewData["Title"] = "Изтрий потребител";
}

<div class="container mt-4">
    <div class="card shadow-sm border-danger">
        <div class="card-header bg-danger text-white">
            <h3 class="mb-0"><i class="fas fa-user-times"></i> @ViewData["Title"]</h3>
        </div>
        <div class="card-body">
            <h5 class="text-danger">Сигурни ли сте, че искате да изтриете този потребител?</h5>
            <hr />

            <div class="row">
                <div class="col-md-6">
                    <strong>Пълно име:</strong>
                </div>
                <div class="col-md-6">
                    @Model.FullName
                </div>
            </div>

            <div class="row mt-2">
                <div class="col-md-6">
                    <strong>Имейл:</strong>
                </div>
                <div class="col-md-6">
                    @Model.Email
                </div>
            </div>

            <div class="row mt-2">
                <div class="col-md-6">
                    <strong>Телефон:</strong>
                </div>
                <div class="col-md-6">
                    @Model.PhoneNumber
                </div>
            </div>

            <hr />
            <form method="post" asp-action="Delete">
                <input type="hidden" asp-for="Id" />

                <div class="d-flex justify-content-end">
                    <button type="submit" class="btn btn-danger me-2">
                        <i class="fas fa-trash-alt"></i> Изтрий
                    </button>
                    <a asp-action="All" class="btn btn-secondary">
                        <i class="fas fa-arrow-left"></i> Назад към списъка
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>
